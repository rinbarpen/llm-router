apiVersion: v1
kind: Service
metadata:
  name: llm-router
  labels:
    app: llm-router
spec:
  selector:
    app: llm-router
  ports:
    - name: http
      port: 80
      targetPort: 18000
      protocol: TCP
  type: ClusterIP
