---
description: 约定与安全规范：编辑 .env / .env.example 时遵循
globs: "**/.env*"
alwaysApply: false
---

# .env 文件规范

## 命名与格式

- 变量名使用 **UPPER_SNAKE_CASE**（如 `LLM_ROUTER_DATABASE_URL`、`OPENAI_API_KEY`）。
- 每行格式：`KEY=value`，等号两侧不要加空格；值含空格或特殊字符时用双引号。
- 用 `#` 做注释，可加分节注释（如 `# === Auth ===`）保持可读。

## 安全

- **不要**在对话或代码里粘贴真实 API Key、密码；用占位符（如 `sk-...`、`your-key`）。
- **不要**把 `.env` 加入版本控制；仅提交 `.env.example` 作为模板。
- 新增需要密钥的配置时，在 `.env.example` 中增加对应键和占位说明，不要写真实值。

## 与项目配置一致

- 本项目中 `src/llm_router/config.py` 从环境变量读取配置；新增或重命名变量时，需同步改 config 与 `.env.example`（以及文档如有说明）。
