# TODO

面向 LLM Router 后续迭代的产品功能待办，按模块与优先级组织。

优先级说明：
- `P0`：高优先级，建议尽快落地
- `P1`：中优先级，建议近期推进
- `P2`：低优先级，可按资源安排

## 渠道与镜像

- [x] `P0` 增加镜像/代理健康检查与熔断策略（失败自动切换可用上游）。
- [x] `P1` 增加渠道级回退链路配置（主渠道失败后按优先级 fallback）。
- [x] `P1` 为镜像与第三方代理补充配置示例与排障文档。

## 路由与负载均衡

- [x] `P0` 为多渠道负载均衡补充可选策略（轮询、加权、最少失败优先）。
- [x] `P1` 增加渠道级实时负载指标（并发、错误率、延迟）并用于路由决策。
- [x] `P2` 支持按模型/渠道的动态权重自动调整（基于近期质量与成本）。

## 流式能力

- [x] `P0` 统一梳理各接口 `stream` 支持范围，并在 API 文档标注能力矩阵。
- [x] `P1` 增加流式连接稳定性优化（超时、断连重试、客户端取消）。
- [x] `P2` 为前端监控增加流式会话观测（首包时间、流式时长、终止原因）。

## 部署与高可用

- [x] `P0` 补齐多机部署文档（反向代理、实例扩缩、会话与状态建议）。
- [x] `P1` 提供参考部署模板（Docker Compose 多实例 / K8s 基础示例）。
- [x] `P1` 增加启动与运行时自检项（数据库、Redis、上游连通性）。

## 令牌与权限治理

- [x] `P0` 完善令牌策略项：过期时间、额度、允许 IP 范围、允许模型/Provider。
- [x] `P1` 增加批量策略下发与模板化配置（按团队/环境复用）。
- [x] `P2` 增加令牌策略变更审计日志（谁在何时改了什么）。

## 额度明细与监控运营

- [x] `P0` 提供额度明细查询视图（按时间、渠道、模型、调用方过滤）。
- [x] `P1` 增加额度预警能力（阈值告警、日/周/月预算提醒）。
- [x] `P1` 增加导出能力（CSV/JSON）用于财务与成本复盘。

## 渠道模型列表管理

- [x] `P0` 支持一键拉取并缓存渠道支持模型列表（含刷新策略）。
- [x] `P1` 支持手动覆盖模型元信息（标签、上下文长度、价格）。
- [x] `P2` 增加模型可用性对账任务（配置模型 vs 实际可调用模型）。

---

## 已完成说明（本轮）

- 已落地渠道可靠性底座：Provider 熔断、失败自动切换、渠道回退链路配置。
- 已落地多渠道负载策略：`round_robin`、`weighted`、`least_failure`。
- 已接入渠道级实时指标并参与路由：并发、失败率、平均延迟（进程内窗口）。
- 已补齐部署与高可用能力：多机部署文档、Compose/K8s 模板、`/health/detail` 运行时自检。
- 已完成令牌治理与额度运营：策略字段、模板批量下发、审计日志、额度明细/导出、预算阈值告警。
- 已完成渠道模型目录管理：上游模型拉取缓存、目录查询、配置与上游模型对账、元信息手动覆盖入口。

## 下一步（建议按序）

1. `P0` 跨实例路由指标聚合与一致性：将并发/失败率/延迟从进程内窗口扩展到共享存储，并在监控页统一展示。
2. `P1` 监控告警闭环：为预算阈值与关键错误增加可配置通知（Webhook）与告警抑制策略。
3. `P1` 性能与可靠性基线：补齐压测场景与 SLO 指标定义（延迟、错误率、可用性），形成回归基准。
4. `P2` 故障演练与恢复手册：沉淀熔断、回退、上游故障场景的演练脚本与 Runbook。
